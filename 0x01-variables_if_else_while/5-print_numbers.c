#include <stdio.h>
/**
 * main - returns 0
 *
 * Description - main returns 0
 *printf- Prints each prints all single digit numbers of base 10
 *Return: 0
 */
int main(void)
{
int s;
for(s = 0; s < 10; s++)
{
printf("%d", s);
}
printf('\n');
return (0);
}
