#include <stdio.h>
/**
 * main - return 0
 * 
 * Description - main return zero in combo
 * Return:0
 */
int main(void)
{
int i, j
for (i = 0; i <= 98; i++)
{
putchar(1 / 10) + '0');
putchar(i % 10) + '0');
putchar(' ');
putchar((j / 10) + '0');
putchar((j % 10) + '0');
if ((i == 98) && (j == 99))
continue;
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
