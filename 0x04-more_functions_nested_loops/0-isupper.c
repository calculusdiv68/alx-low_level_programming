#include "main.h"
/**
 * _isupper - evalute if letter is uppercase .
 *@c: number to be evaluted
 * Return: Always 0.
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}

	else
	{
		return (0);
	}

}
