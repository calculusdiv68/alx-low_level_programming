#include "main.h"
/**
 * _isupper - evalute if letter is uppercase .
 *@c: number to be evaluted
 * Return: Always 0.
 */

int _isupper(char c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}

	else
	{
		return (0);
	}

}
