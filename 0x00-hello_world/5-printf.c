#include <stdio.h>

/**
 *main - execute the file
 *
 *Description - main executes the inputed codes and returns 0
 *printf - prints string with new line
 *Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
